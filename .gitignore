# Dumps do banco (nunca versionar)
*.sql.gz
*.sql.zip
C:/_dump/
C:\_dump\

# Arquivos locais com credenciais
.env.local
*.local

# Instruções locais do Claude Code (contém credenciais e contexto privado)
CLAUDE.md

# Pasta aninhada de sessao anterior (pode ser deletada manualmente)
/DM_Petrvs_icmbio_mysql/

# Exportações brutas do DBeaver (dados operacionais — não versionar)
Tabelas CSV/

# Testes PETRVS/ é versionada (relatórios técnicos de validação — dados públicos de gestão)

# VS Code
.vscode/settings.local.json
*.code-workspace

# Windows
Thumbs.db
desktop.ini

# Apresentações (podem conter dados sensíveis)
*.pptx
*.ppt
*.pdf

# Notebooks Jupyter (podem conter credenciais e dados operacionais)
*.ipynb
